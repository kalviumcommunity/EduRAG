# LearnMate Frontend

Run:

```bash
npm install
npm run dev
```

This is a complete React + Vite frontend prototype with mocked chat responses. Replace the `ask` function in `src/main.jsx` with your backend API call when Member 1/2 are ready.
